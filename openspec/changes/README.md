# Change artifacts

Create one folder per planned change under this directory.

Recommended shape:

```text
openspec/changes/<change-name>/
  proposal.md
  spec.md
  design.md
  tasks.md
```

Use the templates in `openspec/changes/templates/` as a starting point.
